name: CI

# Validate every PR (and pushes to main) before merge: typecheck + production
# build. This is the gate that was missing — PRs like the media/autonomy work
# now get checked automatically instead of relying on local verification.

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install --no-fund --no-audit

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Build
        env:
          NODE_ENV: production
        run: npm run build

  lighthouse:
    # Non-blocking quality signal — builds, serves, and audits the homepage +
    # an article page. continue-on-error so a dip never blocks a merge; results
    # are uploaded to temporary public storage and linked in the job log.
    runs-on: ubuntu-latest
    needs: build
    continue-on-error: true
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install --no-fund --no-audit

      - name: Build
        run: npm run build

      - name: Run Lighthouse
        uses: treosh/lighthouse-ci-action@v12
        with:
          urls: |
            http://localhost:3000/
            http://localhost:3000/pulse
          startServerCommand: npm run start
          uploadArtifacts: true
          temporaryPublicStorage: true
